import { Button, HeroSection, HeroTitle } from "./Home.styled";

const Home = () => {
  return (
    <main>
      <HeroSection>
        <HeroTitle>One click. Long trip</HeroTitle>
        <Button>Start</Button>
      </HeroSection>
    </main>
  );
};

export default Home;
